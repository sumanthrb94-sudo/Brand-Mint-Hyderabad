# cp .env.example .env, then fill it in. .env is gitignored — keep it that way,
# this repository is public.

WA_HOST=wa.brandmintstudios.in

# Generate both, do not invent them:
#   openssl rand -hex 32
EVO_KEY=
PG_PASS=

# Where inbound WhatsApp messages get POSTed. api/wa-hook.js on Vercel.
WEBHOOK_URL=https://brandmintstudios.in/api/wa-hook
