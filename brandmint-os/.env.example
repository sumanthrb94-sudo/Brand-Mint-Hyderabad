# Required in production. Any long random string.
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=

# Only needed once you move off demo data onto Postgres (see README).
DATABASE_URL=
